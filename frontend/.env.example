# Optional. If empty, frontend uses Vite proxy `/api -> http://localhost:8080`.
# You can set:
# 1) VITE_API_BASE_URL=/api
# 2) VITE_API_BASE_URL=http://localhost:8080
# Both will be normalized to `/api` requests.
