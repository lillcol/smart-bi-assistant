# Local config setup

1. Copy `application-local.example.yml` to `application-local.yml`.
2. Fill MySQL username/password.
3. Fill MiniMax and DeepSeek API keys.
4. `application-local.yml` is ignored by git.
